Self :
    def __init__(self, Name, lastName, primary_email, secondary_email, arguments=None, social_media=None):
        self.Name = Name
        self.lastName = lastName
        self.primary_email = primary_email or []
        self.secondary_email = secondary_email or []
        self.arguments = arguments or []
        self.social_media = social_media or {}

    def get_secondary_email(self):
        # This method allows you to access the secondary_email attribute.
        # It's a way to provide controlled access to protected attributes.
        return self.secondary_email

    def set_secondary_email(self, new_secondary_email):
        # This method allows you to modify the secondary_email attribute.
        # Again, it provides controlled access.
        self.secondary_email = new_secondary_email

    def get_social_media(self):
        # This allows you to access the social_media attribute.
        return self.social_media

    def set_social_media(self, new_social_media):
        # This allows you to modify the social_media attribute.
        self.social_media = new_social_media

# Example usage:

    Self = Self(
    Name="Kyle",
    lastName="Santowski",
    primary_email=["kyle@heartofstoneclothing.com", "santowskikr@gmail.com"],
    secondary_email=["santowskikylerobert@gmail.com", "kyle01900@gmail.com"],
    social_media={"twitter": "@kylesantowski", "linkedin": "/in/kylesantowski"}
)

# Accessing and modifying attributes:
print(user.get_secondary_email())  # Access
user.set_secondary_email(["new_email@gmail.com"])  # Modification

print(user.get_social_media())  # Access
user.set_social_media({"twitter": "@new_twitter_handle"})  # Modification
